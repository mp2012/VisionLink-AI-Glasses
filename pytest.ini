[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
addopts = -v --tb=short --strict-markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    jetson: tests that require Jetson hardware (skipped in CI)
    gpu: tests that require GPU (skipped in CI)
